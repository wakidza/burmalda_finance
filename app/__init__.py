"""Бурмалда Finance — приложение для учёта личных финансов.

Пакет содержит слоистую (layered) архитектуру:
    routers      — HTTP-слой (FastAPI), маршрутизация и валидация запросов;
    services     — бизнес-логика (правила предметной области);
    repositories — слой доступа к данным (работа с БД через SQLAlchemy);
    models       — ORM-модели (таблицы БД);
    schemas      — DTO (Pydantic) для входных/выходных данных;
    core         — общие компоненты (исключения, обработчики ошибок).
"""

__version__ = "1.0.0"
